// ===================================
// Aboutページのスクロールアニメーション
// ===================================
jQuery(function ($) {
  // スクロールタイトルアニメーションモジュールを初期化
  if (typeof ScrollTitleAnimation !== "undefined") {
    ScrollTitleAnimation.init(".js-scroll-title");
  }
});


